---
- hosts: vcr
  roles:
    - base
    - seedbox
  vars:
    seedbox_user: netbook

  tasks:
    - name: Install VCR packages
      become: True
      apt: pkg={{item}} state=installed
      with_items:
        - borgbackup
        - ffmpeg
        - lvm2
        - tmux
