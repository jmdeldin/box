---
- name: Prefer IPv4 instead of IPv6 (fixes hanging while connecting to ubuntu servers)
  copy: src=../templates/etc/apt/apt.conf.d/99force-ipv4
        dest=/etc/apt/apt.conf.d/99force-ipv4
  become: True
  tags: [net, packages]

- name: remove garbage packages
  apt: name={{item}} state=absent purge=yes
  become: True
  tags: [packages]
  with_items:
    - brltty # not sure how these packages end up on ubuntu 16.10
    - xbrlapi
    - cheese
    - deja-dup
    - empathy
    - exim4
    - exim4-base
    - exim4-config
    - gnome-mahjongg
    - gnome-mines
    - rhythmbox
    - shotwell
    - thunderbird
    - totem
    - unity-lens-shopping
    - unity-webapps

- name: install basic system packages
  become: True
  apt: name={{item}} state=installed
  with_items:
    - git
    - htop
    - rsync
