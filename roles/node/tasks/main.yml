---
- name: install node
  apt: pkg={{item}} state=installed
  become: True
  with_items:
    - nodejs
    - npm

- name: link node
  file: path=/usr/local/bin/node src=/usr/bin/nodejs state=link
  become: True
